import * as React from "react"

const DarkMode = () => {
    let clickedClass = "clicked";
    const body = document.body;
    
    return(
        <button>

        </button>
    );
};

export default DarkMode